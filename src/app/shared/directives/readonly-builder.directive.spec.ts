import { ReadonlyBuilderDirective } from './readonly-builder.directive';

describe('ReadonlyBuilderDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadonlyBuilderDirective();
    expect(directive).toBeTruthy();
  });
});
